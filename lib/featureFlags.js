// START FEATURE FLAG CHANGE - Feature Flag Definition
/**
 * Central feature flag configuration.
 * To enable a feature, set its value to true.
 * To disable a feature, set its value to false.
 *
 * ACCREDITATION:
 *   - Currently: false (hidden in TEST and PROD — awaiting business approval)
 *   - To enable: change accreditation to true
 */
export const FEATURE_FLAGS = {
  accreditation: false,
};
// END FEATURE FLAG CHANGE
